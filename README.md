# 알데라 대륙경영록

GitHub Pages에 배포하는 정적 사이트입니다.

조직 이름을 `aldera-chronicles`로 만들고, 그 조직에 `aldera-chronicles.github.io`라는 공개 저장소를 생성하세요. 이 폴더의 파일을 저장소의 최상위에 올린 다음, 저장소 Settings → Pages에서 `Deploy from a branch`, `main`, `/ (root)`를 선택하면 됩니다.
